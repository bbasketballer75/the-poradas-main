# Wedding Site Configuration
# Copy this file to .env and fill in your actual credentials
PORT=5000
MONGODB_URI="mongodb+srv://bbasketballer75:passw0rd@wedding-site-cluster.apkobf4.mongodb.net/wedding_site"

# A secret key to protect admin routes. Choose a long, random string.
ADMIN_SECRET_KEY="change-this-to-a-very-secret-key"

# Google Maps API Key for frontend mapping functionality
# Get this from https://console.cloud.google.com/apis/credentials
MAPS_API_KEY="your-google-maps-api-key-here"

# IP Geolocation API Key (if using a paid service instead of free ip-api.com)
# Currently using free ip-api.com which doesn't require a key
# IP_GEOLOCATION_API_KEY="your-geolocation-api-key-here"

# Optional: Node environment
NODE_ENV="development"